<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.acltabontabon</groupId>
    <artifactId>vortex-parent</artifactId>
    <version>0.1.0-alpha.16</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>vortex-app</artifactId>
  <name>Vortex :: App</name>
  <description>Composition root: Spring Boot entry point, web UI, SSE, local lab and observability adapters.</description>

  <dependencies>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-core</artifactId></dependency>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-openapi</artifactId></dependency>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-k6</artifactId></dependency>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-ai</artifactId></dependency>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-dynatrace</artifactId></dependency>
    <dependency><groupId>com.acltabontabon</groupId><artifactId>vortex-persistence</artifactId></dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-restclient</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- @WebMvcTest lives in its own starter as of Spring Boot 4. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.acltabontabon</groupId>
      <artifactId>vortex-core</artifactId>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
    </dependency>
  </dependencies>

  <build>
    <finalName>vortex</finalName>
    <resources>
      <!--
        application.yaml carries a literal @project.version@ token so the Settings page can show
        the real released version instead of a hand-maintained (and inevitably stale) string. The
        `@` delimiter, not Maven's default `${...}`, is deliberate: this file already uses `${...}`
        extensively for Spring's own runtime placeholder resolution (${user.home},
        ${vortex.ai.base-url}, ...) — filtering those at build time with the default delimiter would
        silently corrupt them. Everything else (notably the built frontend's binary/JS/CSS assets
        under static/app/) stays unfiltered, since filtering binary files corrupts them too.
      -->
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>application.yaml</include>
        </includes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>application.yaml</exclude>
        </excludes>
      </resource>
    </resources>
    <plugins>
      <!--
        SQLite's JDBC driver loads a native library. On JDK 25 that produces a restricted-method
        warning on every start unless native access is granted, which would make `vortex doctor`
        output begin with four lines of noise about something the user cannot act on.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifestEntries>
              <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <configuration>
          <useDefaultDelimiters>false</useDefaultDelimiters>
          <delimiters>
            <delimiter>@</delimiter>
          </delimiters>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.acltabontabon.vortex.app.VortexApplication</mainClass>
        </configuration>
      </plugin>
      <!--
        The homepage (ADR-035). Builds web (React + Mantine) into
        src/main/resources/static/app/ before resources are processed, so `mvn package` still
        produces one self-contained jar. Node is downloaded into web/node/ for this build
        only — nothing is installed on the host.
      -->
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>1.15.1</version>
        <configuration>
          <workingDirectory>${project.basedir}/../../web</workingDirectory>
          <installDirectory>${project.basedir}/../../web</installDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <!-- Vite 8 / rolldown require Node ^20.19.0 || >=22.12.0; anything below that also
                   breaks npm's native optional-dependency resolution (npm/cli#4828). -->
              <nodeVersion>v22.14.0</nodeVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-ci</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>ci</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <configuration>
              <imageName>vortex</imageName>
              <buildArgs>
                <buildArg>--enable-http</buildArg>
                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
              </buildArgs>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
